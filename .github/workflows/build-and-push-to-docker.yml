name: learn-github-actions
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    name: Checkout code
    steps:
      - uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Build client
        run: docker build -t joshcausey/client ./client

      - name: Build server
        run: docker build -t joshcausey/server ./worker

      - name: Build worker
        run: docker build -t joshcausey/worker ./worker
      
      - name: Push imaages to Docker Hub
        run: |
          docker push joshcausey/client
          docker push joshcausey/server
          docke rpush joshcausey/worker